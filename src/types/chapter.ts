export type ChapterText = {
    observe: string;
    uncertainty: string;
    misreflection: string;
    response: string;
    exit: string;
};

export type Chapter = {
    id: string;
    image: string;
    characters: string[];
    sentences: string;
};